# Secrets — NEVER commit these
# (comments must be on their own line; a trailing '#' is treated as
#  literal pattern text by git, which silently breaks the ignore rule)
saxo_token.json
saxo_token_live.json
.env
*.env
.env.binance
.env.live
# FTP credentials for the (legacy) dashboard upload
config/deploy.json
# Email credentials (Gmail app password) — copy email.json.template -> email.json to enable
config/email.json
# Binance — the .env.binance file holds testnet API keys
# (already caught by *.env above, but listed explicitly for clarity)

# ── Runtime state / databases — local only, not history ──────────
# SQLite databases (large, machine-local)
data/atos.db
data/atos_live.db
data/atos_live.db-shm
data/atos_live.db-wal
# LIVE stocks sleeve (atos_live_stocks.py) — runtime state, machine-local
data/atos_live_stocks.db
data/atos_live_stocks.db-shm
data/atos_live_stocks.db-wal
data/atos_live_stocks_risk_state.json
data/us_momentum_state_live.json
data/us_blend_live_would_be_orders.jsonl
data/stocks_live_status.json
data/stocks_live_scheduler.log
# LIVE instrument map is operator-built (lookup_instruments_live.py), not history
data/instrument_map_live.csv
# Log files
data/monitor_log.txt
data/*.log
data/engine_*.log
# risk / daily capital trackers (regenerate automatically)
data/atos_risk_state.json
data/daily_state.json
data/risk_capital.json
data/us_momentum_state.json
data/atos_scan_state.json
# Binance equity stop state (peak equity + halt status; machine-local, gitignored)
data/binance_equity_state.json
# AI advisory layer -- runtime logs (shadow decisions / proposals / advisor / journal), machine-local
data/ai_shadow_decisions.jsonl
data/ai_trade_proposals.jsonl
data/ai_trade_journal.jsonl
data/exit_advisor_shadow.jsonl
data/rsi_signal_registry.jsonl
# Stocks AI layer runtime logs (2026-09-02) -- equity observation cards + Blend basket shadow
data/stock_observation_cards.jsonl
data/ai_basket_shadow.jsonl
# "ATOS needs a human" escalation channel -- ephemeral, safeguard rewrites it every cycle
data/attention_state.json
# last-good Saxo FX rate cache -- ANALYTICS-ONLY fallback for R-multiple / cost
# enrichment when Saxo can't price a quote ccy on a given run; machine-local,
# rebuilds itself from live quotes (forex/runner._save_eur_rate)
data/eur_rate_cache.json
# strategy monitor state
data/strategy_monitor.json
# gap strategy cooldown — per-week, per-pair re-entry block (runtime state, machine-local)
data/gap_cooldown.json
data/gap_weekend_cooldown.json
data/lbo_v2_session_cooldown.json
# Real-money LIVE forex account runtime state (positions/orders/peak-equity) --
# machine-local, never source-controlled, same as the other risk/state trackers above
data/forex_live_state.json
data/forex_live_orders.json
data/forex_live_peak_equity.json
# SIM module runtime state — positions / caches / perf logs rewritten every
# cycle, reconciled against live Saxo on load, and every reader guards a
# missing file. Untracked 2026-09-02 (were committed by mistake, dirtied
# git status on every run). Same class as the LIVE state above.
# (data/attention_state.json is covered a few lines up.)
data/forex_state.json
data/futures_state.json
data/futures_uic_cache.json
saxo_etf_strategy/data/etf_positions.json
saxo_etf_strategy/data/etf_rank_performance_log.csv
# Cross-process lock files -- runtime-only, never meaningful across machines
*.lock
# generated HTML dashboard (was uploaded to namazic.com)
dashboard/

# Python
__pycache__/
*.pyc
*.pyo
atos/__pycache__/

# Regenerable data — large historical price CSVs, re-downloadable via data_loader.py
data/*.csv
!data/instrument_map.csv
!data/index_map.csv

# Runtime
results/
STOP_TRADING

# Virtual environment
.venv/
venv/

# Log files — never commit; large and machine-local
logs/

# Kill-switch sentinel files (written at runtime, not source-controlled)
STOP_BINANCE

# Backtest cache pickles (re-downloadable via --reset)
data/*.pkl

# Daily strategy charts — regenerated every day via daily_chart.py, machine-local
data/charts/

# CNN-LSTM model artifacts (large, regenerated via --train)
data/cnn_lstm/model.pt
data/cnn_lstm/scaler.json
data/cnn_lstm/report.json
data/cnn_lstm/config.json

# ── Runtime artifacts — 2026-09-02 sweep (git status was showing ~70 of these) ──
# Stray root-level DB (duplicate of data/atos_live.db, already ignored above)
/atos_live.db
# P&L ledger SQLite DB (machine-local, same class as data/atos_live.db)
data/pnl_ledger.db
data/pnl_ledger.db-shm
data/pnl_ledger.db-wal
# Every timestamped backup anywhere under data/ (*.db.bak_…, *.json.bak_…, *.jsonl.bak_…)
data/*.bak_*
# Test-run scratch databases / state files
data/_test_*
# Scheduler fallback logs — written only when the real logger can't open its file
data/*.log.fallback
# Performance trackers — regenerated every run by the *_performance_tracker jobs
data/*_performance_tracker.xlsx
data/forex_live_capital_analysis.xlsx
# RSI tracker workbook — regenerated every run by reports/rsi_tracker.py
data/rsi_tracker.xlsx
# Runtime JSONL streams (observation cards, AI shadow baskets, equity curve,
# cost-gate decisions, currency-exposure snapshots) — machine-local, append-only
data/*.jsonl
# Machine-local runtime state written on every cycle
data/forex_watchdog_state.json
data/watchdog_state.json
data/safeguard_alert_state.json
data/atos_status.json
data/forex_live_eur_state.json
data/forex_live_eur_orders.json
data/forex_live_eur_peak_equity.json
data/forex_live_eur_strategy_weights.json
data/forex_live_strategy_weights.json
data/forex_orders.json
data/forex_peak_equity.json
data/forex_strategy_weights.json
data/futures_orders.json
data/futures_strategy_weights.json
data/futures_all_results.json
# Open-scan / run-now sentinel files
data/open_scan_*.flag
data/run_now.txt
# Generated reports, raw API caches, patched Task Scheduler exports
data/validation_report.txt
data/test_marker_check.txt
data/*_task_fixed.xml
data/saxo_currencypairs_raw.json
data/saxo_new_pairs_details.json
data/strategy_test_logs/
# Archives / exports dropped in the tree
*.zip

# OS clutter
.DS_Store
Thumbs.db
.devtools/
